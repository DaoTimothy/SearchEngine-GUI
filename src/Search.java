import java.io.*;
public class Search {
    private File directory;
    public Search(String directoryName) {
        directory = new File(directoryName);
    }

    public void search(String phrase, boolean boost) {

    }
}
